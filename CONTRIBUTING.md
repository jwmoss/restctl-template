# Contributing

## Local Setup

```bash
brew install uv
make test
```

## Pull Requests

- Keep changes scoped to the template contract.
- Render a project locally before opening a PR.
- Run `make check` inside the rendered project.
- Update docs when adding or changing prompts.

## Generated Project Quality Bar

The default rendered project must build, test, and produce useful help output
without network access.
